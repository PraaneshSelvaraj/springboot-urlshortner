version: "3.9"

services:
  mysql:
    image: mysql:latest
    container_name: mysql-service
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5

  notification-service:
    build:
      context: ./services
      dockerfile: notification-service/Dockerfile
    container_name: notification-service
    ports:
      - "8081:8081"
      - "9091:9091"
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      NOTIFICATION_SERVICE_PORT: ${NOTIFICATION_SERVICE_PORT}
      NOTIFICATION_GRPC_PORT: ${NOTIFICATION_GRPC_PORT}
    depends_on:
      mysql:
        condition: service_healthy
    restart: on-failure

  rest-service:
    build:
      context: ./services
      dockerfile: rest-service/Dockerfile
    container_name: rest-service
    ports:
      - "8080:8080"
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      GRPC_NOTIFICATION_HOST: notification-service
      GRPC_NOTIFICATION_PORT: ${NOTIFICATION_GRPC_PORT}
      GRPC_USER_HOST: user-service
      GRPC_USER_PORT: ${USER_GRPC_PORT}
      BANNED_HOSTS: ${BANNED_HOSTS}
      URL_EXPIRATION_HOURS: ${URL_EXPIRATION_HOURS}
      NOTIFICATION_THRESHOLD: ${NOTIFICATION_THRESHOLD}
      APP_BASE_URL: ${APP_BASE_URL}
      REST_SERVICE_PORT: ${REST_SERVICE_PORT}
    depends_on:
      mysql:
        condition: service_healthy
      notification-service:
        condition: service_started
    restart: on-failure

  user-service:
    build:
      context: ./services
      dockerfile: user-service/Dockerfile
    container_name: user-service
    ports:
      - "8082:8082"
      - "9092:9092"
    environment:
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      JWT_ACCESS_TOKEN_EXPIRATION: ${JWT_ACCESS_TOKEN_EXPIRATION}
      JWT_REFRESH_TOKEN_EXPIRATION: ${JWT_REFRESH_TOKEN_EXPIRATION}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      USER_SERVICE_PORT: ${USER_SERVICE_PORT}
      USER_GRPC_PORT: ${USER_GRPC_PORT}
    depends_on:
      mysql:
        condition: service_healthy
    restart: on-failure

volumes:
  mysql-data:
